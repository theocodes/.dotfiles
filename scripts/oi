#!/bin/sh

set -e

sync () {
    echo
    echo -e "\n==== Pushing changes ===="
    git add .
    git commit -m "$(date)"
    git push -u origin master
    echo "==== done ===="
    echo
}

if [[ "$1" = "sync" ]]; then
    sync
else
    pushd ~/dotfiles
    make $1
    sync
    popd
fi
